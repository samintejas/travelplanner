# OpenAI API Key (required for AI features)
OPENAI_API_KEY=sk-your-api-key-here

# Database (used by docker-compose, no need to change)
DATABASE_URL=postgresql://travel:travel123@localhost:5432/travel_concierge

# ChromaDB (used by docker-compose, no need to change)
CHROMA_HOST=localhost
CHROMA_PORT=8001
